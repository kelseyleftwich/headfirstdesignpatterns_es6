import {override} from '../ClassUtilities';

class Subject {
    registerObserver() {
        override();
    }

    removeObserver() {
        override();
    }

    notifyObservers() {
        override();
    }
}

export default Subject;